import type { ProjectConfig } from "../types";

/**
 * Stub: integrate Clarity/Hotjar later (heatmaps, rage clicks, scroll depth).
 */
export async function fetchClarityMetrics(_cfg: ProjectConfig): Promise<Record<string, any>> {
  return {};
}
