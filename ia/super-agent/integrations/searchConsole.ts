import type { ProjectConfig } from "../types";

/**
 * Stub: integrate Search Console later (queries, clicks, ctr, avg position).
 */
export async function fetchSearchConsoleMetrics(_cfg: ProjectConfig): Promise<Record<string, any>> {
  return {};
}
